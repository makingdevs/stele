<%= form_for(@dependiente) do |f| %>
  <% if @dependiente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dependiente.errors.count, "error") %> prohibited this dependiente from being saved:</h2>

      <ul>
      <% @dependiente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :perfil_id %><br />
    <%= f.number_field :perfil_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
